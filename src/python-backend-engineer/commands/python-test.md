# Command: Test

Run Python tests with coverage reporting.

## Usage
```
/test [options]
```

## Examples
```bash
/test                    # Run all tests
/test --unit            # Run unit tests only
/test --coverage        # Run with coverage report
/test --file tests/test_api.py  # Run specific file
```
